<%= form_for(@product_owner) do |f| %>
  <% if @product_owner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_owner.errors.count, "error") %> prohibited this product_owner from being saved:</h2>

      <ul>
      <% @product_owner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
